<main class="max-w-3xl mx-auto bg-white shadow-md rounded-lg p-8">
  <h1 class="text-3xl font-bold text-blue-600 mb-6">Dashboard</h1>
  <p class="text-gray-700 mb-6">Welcome, <span class="font-semibold">{{user.username}}</span>!</p>
  <form action="/logout" method="GET">
    <button
      type="submit"
      class="w-full bg-red-500 text-white py-2 px-4 rounded hover:bg-red-600"
    >
      Logout
    </button>
  </form>
</main>
